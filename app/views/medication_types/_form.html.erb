<%= form_with(model: medication_type) do |form| %>
  <% if medication_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(medication_type.errors.count, "error") %> prohibited this medication_type from being saved:</h2>

      <ul>
        <% medication_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
